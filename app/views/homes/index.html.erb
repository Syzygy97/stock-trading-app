<ul>
  <li><%= link_to "Portfolio Index Page", portfolios_index_path %></li>
  <li><%= link_to "Stock Index Page", stocks_index_path %></li>
  <% if user_signed_in? %>
    <li><%= link_to "Sign Out", destroy_user_session_path, data: { "turbo-method": :delete } %></li>
  <% elsif !admin_signed_in? %>
    <li><%= link_to "Sign In", new_user_session_path %></li>
  <% end %>

  <% if admin_signed_in? %>
    <li><%= link_to "Sign Out as Admin", destroy_admin_session_path, data: { "turbo-method": :delete } %></li>
  <% elsif !user_signed_in? %>
    <li><%= link_to "Sign In as Admin", new_admin_session_path %></li>
  <% end %>
</ul>

<h1>Home Page</h1>
<p>Find me in app/views/homes/index.html.erb</p>